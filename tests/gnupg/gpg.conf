# The tests verify signatures made by keys that are not in this keyring on purpose, make sure GnuPG doesn't try to
# fetch them, that would make the tests depend on the network and on whatever the machine has in its global config.
no-auto-key-retrieve
keyserver-options no-auto-key-retrieve
